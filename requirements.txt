# ── Kafka ─────────────────────────────────────────────────────────────────────
confluent-kafka==2.3.0
fastavro==1.12.2

# ── Alpaca market data ────────────────────────────────────────────────────────
alpaca-py==0.21.1

# ── Stream Processing (Faust — built by Robinhood for financial data) ──────────
faust-streaming==0.11.3

# ── Storage — DuckDB + Iceberg + Delta Lake + MinIO ───────────────────────────
duckdb==0.10.3
deltalake==0.17.4
pyarrow==15.0.2
pandas==2.2.1
boto3==1.34.0
pyiceberg[sql-sqlite]==0.7.1   # pinned: 0.8+ needs pyarrow>=16, project is on 15

# ── AI / RAG (Week 3) ─────────────────────────────────────────────────────────
langchain
langchain-core
langchain-openai
langchain-community==0.0.38
chromadb==0.5.0
openai==1.30.1

# ── dbt (Week 2) ──────────────────────────────────────────────────────────────
dbt-core==1.8.0
dbt-duckdb==1.8.0

# ── Utils ─────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1
loguru==0.7.2
requests==2.31.0
fastapi==0.111.0
uvicorn==0.30.0
pytest==8.2.2
pytest-asyncio==0.23.5
pytest-cov==5.0.0