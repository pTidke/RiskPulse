version: 2

models:
  - name: stg_risk_metrics
    description: "Cleaned and deduplicated VWAP windows from Faust"
    columns:
      - name: symbol
        data_tests: [not_null]
      - name: window_start
        data_tests: [not_null]
      - name: vwap
        data_tests: [not_null]
      - name: trade_count
        data_tests: [not_null]
