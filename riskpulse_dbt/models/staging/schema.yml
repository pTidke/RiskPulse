version: 2

models:
  - name: stg_risk_metrics
    description: "Cleaned and deduplicated VWAP windows from Faust"
    columns:
      - name: symbol
        tests: [not_null]
      - name: window_start
        tests: [not_null]
      - name: vwap
        tests: [not_null]
      - name: trade_count
        tests: [not_null]
